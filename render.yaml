services:
  - type: web
    name: Raider-Bot
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    runtime: python3.10
